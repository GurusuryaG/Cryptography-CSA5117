def left_shift(block):
    return ((block << 1) & ((1 << block.bit_length()) - 1))

def generate_subkey(L, Rb):
    if L & (1 << (L.bit_length() - 1)):
        return (L << 1) ^ Rb
    return L << 1

print("CMAC constants:")
print("64-bit block : Rb = 0x1B")
print("128-bit block: Rb = 0x87")

L = int(input("Enter L in hexadecimal: "), 16)

K1 = (L << 1) & ((1 << 128) - 1)

if L & (1 << 127):
    K1 ^= 0x87

K2 = (K1 << 1) & ((1 << 128) - 1)

if K1 & (1 << 127):
    K2 ^= 0x87

print("K1 =", hex(K1))
print("K2 =", hex(K2))
