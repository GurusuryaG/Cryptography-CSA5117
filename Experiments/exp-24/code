import math

n = 3599
e = 31

for p in range(2, n):
    if n % p == 0:
        q = n // p
        break

phi = (p - 1) * (q - 1)

d = pow(e, -1, phi)

print("p =", p)
print("q =", q)
print("phi(n) =", phi)
print("Private key d =", d)
